<div class="mx-auto flex w-full max-w-5xl flex-col gap-6">
  <%= render FlatPack::PageNav::Component.new(
    anchor_url: main_app.root_path
  ) do |nav| %>
    <% nav.right do %>
      <%= link_to settings_path do %>
        <%= render FlatPack::Button::Component.new(
          icon: "cog",
          icon_only: true,
          style: :secondary,
          size: :md,
          aria: { label: "Settings" }
        ) %>
      <% end %>
    <% end %>
  <% end %>

  <%= render FlatPack::PageTitle::Component.new(
    title: "Notifications",
    variant: :h1
  ) %>

  <% if @notifications.any? %>
    <%= render FlatPack::List::Component.new(spacing: :comfortable, divider: true) do %>
      <div id="notifications-list">
        <%= render partial: "recording_studio_notifications/notifications/page", locals: { notifications: @notifications } %>
      </div>
    <% end %>

    <div class="pt-2">
      <%= render partial: "recording_studio_notifications/notifications/next_page", locals: { page: @page, has_next_page: @has_next_page } %>
    </div>
  <% else %>
    <%= render FlatPack::Card::Component.new(style: :default) do |card| %>
      <% card.body do %>
        <p class="text-sm text-(--surface-muted-content-color)">No notifications yet.</p>
      <% end %>
    <% end %>
  <% end %>
</div>
